
<div class="page-header">
  <h1><%=t '.title', :default => @employee.name %></h1>
</div>

<div class="form-actions">
  <%= link_to 'Edit', edit_employee_path(@employee) %> |
  <%= link_to 'Back', employees_path %>
</div>
